# Arquivo de exemplo para configuração de variáveis de ambiente
# Copie este arquivo para .env e preencha os valores conforme necessário

# Configurações do servidor
PORT=8000
PYTHONPATH=/opt/render/project/src

# Configurações de segurança
API_KEY_SALT="salt-secreto-mude-em-producao"  # Salt usado para hashear API keys
API_KEYS='[{"key": "your-api-key", "role": "admin"}, {"key": "test-api-key", "role": "read-only"}]'

# Configurações de rate limiting
RATE_LIMIT_WINDOW=60  # Tempo em segundos para o rate limiting
RATE_LIMIT_ADMIN=100  # Requisições permitidas para admin no período
RATE_LIMIT_READONLY=30  # Requisições permitidas para read-only no período

# Configurações de logs
LOG_LEVEL=INFO
LOG_FILE=api_logs.log